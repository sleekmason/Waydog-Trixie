#!/bin/bash
# A simple script to set the environmental variables in advance in order to use root.
if [[ "$XDG_SESSION_TYPE" == "wayland" ]]; then
pkexec env WAYLAND_DISPLAY=$WAYLAND_DISPLAY XDG_RUNTIME_DIR=$XDG_RUNTIME_DIR $@
else
pkexec env DISPLAY=$DISPLAY XAUTHORITY=$XAUTHORITY $@
fi
