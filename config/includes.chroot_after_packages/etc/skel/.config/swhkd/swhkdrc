# Openbox sxhkdrc
# These are the keybinds used in openbox.
#########################################################

# Kill and start waybar
mod4 + w
	waybar-toggle

# sxhkd keybind list through a rofi dialog.
mod4 + k
	show-keybinds
    
# Reload swhkd configuration.
mod4 + Escape
	pkill -USR1 -x swhkd; notify-send --urgency low 'swhkd configuration reloaded'
	
# Fuzzel.
mod4 + F5
	fuzzel

# Conky.
mod4 + c
	conky-toggle

# theme-changer.
mod4 + a
	ld-themes

# terminal.
mod4 + Return
	x-terminal-emulator

# terminal.
mod4 + t
	x-terminal-emulator
    
# File Manager.	
mod4 + f
	thunar

# File Manager split view.
shift + mod4 + f
	splitview
	
# File Manager split vertical-horizontal.
mod4 + s
	xfconf-query -c thunar -p /misc-vertical-split-pane -T

# Editor.
mod4 + e
	geany
    
# Web browser.
mod4 + b
	x-www-browser

# Logout.
mod4 + x
	ld-logout
	
#Volume Controls
XF86AudioLowerVolume
	 pamixer --decrease 5 && notify-send "Volume down" -u low -t 500 
XF86AudioRaiseVolume
	 pamixer --increase 5 && notify-send "Volume up" -u low -t 500
XF86AudioMute
	 pamixer --toggle-mute && notify-send "Mute/Unmute" -u low -t 2000

#Brightness controls
XF86MonBrightnessDown
	brightnessctl s 10%-

XF86MonBrightnessUp
	brightnessctl s 10%+			

# screenshot and selection capture.
Print
	grimshot save area
	
